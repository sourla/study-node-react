import type { Post } from '../types/post'
export function PostList({ posts }: { posts: Post[] }) { return <ul aria-label="게시글 목록" className="grid gap-4 md:grid-cols-2">{posts.map((post) => <li className="rounded-2xl border border-white/10 bg-white/[.04] p-6 transition hover:-translate-y-0.5 hover:border-violet-400/40" key={post.id}><div className="mb-4 flex items-center justify-between"><span className="rounded-full bg-violet-400/10 px-2.5 py-1 text-xs font-medium text-violet-300">POST #{post.id}</span><span className="text-xs text-zinc-500">{post.createdAt}</span></div><h2 className="mb-2 text-xl font-semibold text-white">{post.title}</h2><p className="mb-6 leading-7 text-zinc-400">{post.content}</p><small className="text-zinc-500">by {post.author}</small></li>)}</ul> }
